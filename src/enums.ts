enum sortOrderEnum {
  DEFAULT = 'default',
  ASC = 'asc',
  DESC = 'desc'
}

enum cellTypeEnum {
  TEXT = 'text',
  NUMBER = 'number',
  ARRAY = 'array',
  IMAGE = 'image'
}

export { sortOrderEnum, cellTypeEnum };